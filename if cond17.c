#include<stdio.h>
int main() {
    int a=15;
    if (a>=10 && a<=20) {
        printf("Number within range");
    }
    else {
        printf("Number does'n in the range");
    }
    return 0;
}