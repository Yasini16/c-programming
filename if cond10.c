#include<stdio.h>
int main() {
    int a=345;
    if (a>=100 && a<=999) {
        printf("Three-digit Number");
    }
    else {
        printf("Not a Three-digit Number");
    }
    return 0;
}